# Compat shim — canonical dependency manifest is pyproject.toml + uv.lock.
#
# Prefer (contributors):  uv sync --group dev
# Prefer (end users):     ./install.sh
# Classic pip fallback:   pip install -r requirements.txt
#
# Optional extras (not installed by default):
#   pip install -e ".[embeddings]"   # in-process sentence-transformers (pulls torch)
#
-e .
