.DS_Store
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtualenvs
.venv/
venv/

# Env files (keep examples)
.env
.env.*
!.env.example

# Local runtime logs/state
.braindrain/
*.log

# QA / installer scratch (local-only)
QA-Logs/
.tmp/
.pytest_cache/
.ruff_cache/

# Dev docs dir (local-only; excluded from public repo)
.devdocs/

# AGENTS.md is generated per device by install.sh / refresh_env_context()
# It contains personal env data (hostname, IPs, paths, IDE configs)
AGENTS.md
# Keep AGENTS.md.template tracked (it's the canonical protocol template)
!AGENTS.md.template

# ── Ruler-generated agent config files (source: config/templates/ruler/) ──────
# These are generated by prime_workspace() / ruler apply.
# Source templates live in config/templates/ruler/ — those ARE committed.
CLAUDE.md
CLAUDE.md.bak
.cursor/
.cursor/rules/
.cursor/mcp.json
.cursor/mcp.json.bak
.clinerules
.clinerules.bak
.windsurf/
.windsurf/rules/
.windsurf/mcp.json
.windsurf/mcp_config.json
.windsurf/mcp_config.json.bak
.vscode/
.github/copilot-instructions.md
.kiro/
.opencode/
.zed/
.amp/
.goose/
.warp/
.roo/
.aider/
.mcp.json
.mcp.json.bak
.trae/rules/
.trae/rules/*.bak
.agent/
.aiassistant/
.amazonq/
.augment/
.codex/
.debug-reports/
.factory/
.gemini/
.idx/
.junie/
.kilocode/
.openhands/
.qwen/
.vibe/
.ruler/
AGENTS.md
AGENTS.md.bak
CLAUDE.md
CLAUDE.md.bak
WARP.md
WARP.md.bak
.goosehints
.goosehints.bak
opencode.json
opencode.json.bak
.vscode/mcp.json
.vscode/settings.json
config/opencode_mcp.json
config/zed_mcp.json
firebender.json
firebender.json.bak
.crush.json
.aider.conf.yml.bak
.aider.conf.yml
BRAIN_MCP_HUB.code-workspace
config.toml
config.toml.bak
CRUSH.md

# Tests are dev-only (not needed on consumer installs)
tests/


# START Ruler Generated Files
/.cursor/mcp.json
/.cursor/mcp.json.bak
/AGENTS.md
/AGENTS.md.bak
# END Ruler Generated Files

# BEGIN BRAINDRAIN GITIGNORE PROTOCOL — do not remove (maintained by prime_workspace)
# Ruler does NOT own .gitignore here: we pass --gitignore false to ruler apply and maintain this block instead.
# Root-level dotfiles/dotdirs are local-only (IDE, MCP, secrets). Add ! exceptions only for paths that must ship.
/.*
!/.github/
!/.gitignore
!/.gitattributes
!/.gitmodules
!/.env.example
# If your team commits other root dotdirs (e.g. .husky), add: !/.husky/
# END BRAINDRAIN GITIGNORE PROTOCOL
