name: core
version: "1"
description: Core priming bundle with essential protocol surfaces.
agents:
  - cursor
rules:
  - braindrain
skills: []
mcp_servers:
  - braindrain
capability_profiles:
  default_role: coordinator
  roles:
    coordinator:
      tool_profile: core
