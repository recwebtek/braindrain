# AGENTS.md — BRAINDRAIN minimal protocol

## Rules (always)

1) **Discover first**: call `search_tools(query, top_k=...)` before repo-wide search/reading.
2) **Route big outputs**: use `route_output(text, source, intent=...)`; then retrieve via `search_index(query, limit=...)` (or `ctx_search`).
3) **Measure**: call `get_token_dashboard()` (or `get_token_stats()`) at milestones.
4) **Keep docs current**: if behavior/run paths/tools change, update `docs/SESSION_PROGRESS.md`, `docs/OPS.md`, and `README.md`.

## BRAINDRAIN hot tools (current)

- `search_tools`, `route_output`, `search_index`, `get_token_dashboard`, `get_token_stats`, `get_available_tools`, `ping`, `get_env_context`

## Environment Context Protocol

Before running any shell commands, installing packages, or assuming tool
availability — call `get_env_context()` first.

This returns a cached OS fingerprint with zero token overhead after the first
probe. It tells you:
- Exact hostname, username, and LAN IPs (no guessing `sudo` user vs login user)
- Which package manager to use (`brew` vs `apt` vs `dnf` etc.)
- Which modern CLI tools are installed (`fd`, `bat`, `rg`, `fzf`...)
- Active runtimes and their version managers (`nvm`, `pyenv`, etc.)
- Shell type and config path
- Agent behaviour hints (what to prefer and what to avoid on this OS)

**Never probe the environment yourself** (no `which`, `command -v`, `uname`
chains). The cached context is authoritative. If something seems missing,
call `refresh_env_context()` and the user will confirm.

<!-- ENV_CONTEXT_START -->
## OS Environment Context
<!-- generated by braindrain env_context — 2026-03-19 -->

- **User**: `ettienne` on `ECMacMini`
- **LAN IPs**: `192.168.2.1`, `192.168.1.200`
- **OS**: macOS — ProductName:		macOS · arm64
- **Kernel**: `24.1.0`
- **Hardware**: Apple M1 · 8 GB RAM
- **Shell**: `/bin/zsh` · terminal: `zed`
- **Package manager**: `/opt/homebrew/bin/brew` (also: brew)
- **Runtimes**: `python3` 3.12.9 · `node` v24.9.0 · `ruby` [universal.arm64e-darwin24] · `java` Runtime.
- **Python interpreters in PATH**:
  - `/opt/homebrew/Caskroom/miniforge/base/bin/python3` → Python 3.12.9
  - `/Users/ettienne/.pyenv/shims/python3` → Python 3.8.18
  - `/usr/local/bin/python3` → Python 3.12.8
  - `/usr/bin/python3` → Python 3.9.6
  - `.venv/bin/python3` → Python 3.12.11
  - ⚠️ pyenv active: `3.8.18 (set by /Users/ettienne/.pyenv/version)` — bare `python3` goes through shims
- **Containers**: `docker` 29.2.1, `docker_compose` Docker Compose version v5.1.0
- **Editors**: `/usr/bin/vim`, `vim`, `/usr/bin/nano`, `nano`, `/usr/local/bin/cursor`, `cursor`, `/Users/ettienne/.codeium/windsurf/bin/windsurf`, `windsurf`
- **Key CLI tools**: `/opt/homebrew/bin/rg`, `rg`, `/usr/bin/jq`, `jq`, `/opt/homebrew/bin/gh`, `gh`
- **Git**: git version 2.39.5 (Apple Git-154) · user.name=ettienne, user.email=12529192+jhweb@users.noreply.github.com
- **IDE/Agent MCP configs**:
  - **Cursor** → `/Users/ettienne/.cursor/mcp.json` · servers: `desktop-commander`, `MCP_DOCKER`, `supabase`, `braindrain`
  - **Windsurf** → `/Users/ettienne/.codeium/windsurf/mcp_config.json` · servers: `mcp-playwright`, `puppeteer`, `sequential-thinking`
  - **Zed** → `/Users/ettienne/.config/zed/settings.json` · servers: `serena-context-server`, `braindrain`
  - **OpenCode** → `/Users/ettienne/.config/opencode/opencode.jsonc` · servers: _(none active)_
  - **Antigravity** → `/Users/ettienne/.gemini/antigravity/mcp_config.json` · servers: `sequential-thinking`, `braindrain` · disabled: `firebase-mcp-server`, `supabase-pileapp`
  - **Gemini CLI** → `/Users/ettienne/.gemini/settings.json` · servers: `MCP_DOCKER`
- **No MCP config found for**: Claude, Claude (alt), Codex, Continue, VS Code
- **Agent notes**: prefer `rg` over `grep` · use `/opt/homebrew/bin/brew` for package installs · macOS — avoid Linux-only flags (e.g. `--no-preserve-root`, `date -d`) · MULTIPLE python3 interpreters in PATH — verify venv is active before running python. Found: /opt/homebrew/Caskroom/miniforge/base/bin/python3 → Python 3.12.9; /Users/ettienne/.pyenv/shims/python3 → Python 3.8.18; /usr/local/bin/python3 → Python 3.12.8; /usr/bin/python3 → Python 3.9.6; .venv/bin/python3 → Python 3.12.11 · pyenv active: 3.8.18 (set by /Users/ettienne/.pyenv/version) — bare `python3` resolves through pyenv shims
<!-- ENV_CONTEXT_END -->
